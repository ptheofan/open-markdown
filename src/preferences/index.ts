// Preference defaults and utilities
export {
  PREFERENCES_VERSION,
  DEFAULT_CORE_PREFERENCES,
  DEFAULT_MERMAID_PREFERENCES,
  DEFAULT_APP_PREFERENCES,
  clonePreferences,
  deepMerge,
} from './defaults';

export type { MermaidPreferences } from './defaults';
